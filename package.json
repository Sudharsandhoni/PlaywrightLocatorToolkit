{
  "name": "playwright-locator-toolkit-workspace",
  "version": "0.2.2",
  "private": true,
  "description": "Live Playwright Locator Validation & Analysis VSCode Extension",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=packages/browser-agent && npm run build --workspace=packages/engine && npm run build --workspace=packages/extension",
    "build:shared": "npm run build --workspace=packages/shared",
    "watch": "npm run watch --workspaces"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.3.3"
  }
}
